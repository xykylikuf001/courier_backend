<mjml>
  <mj-body background-color="#fff">
    <mj-section>
      <mj-column>
        <mj-divider border-color="#555"></mj-divider>
        <mj-text font-size="20px" color="#555" font-family="helvetica">
        {{ project_name }} - {{_("Email verification")}}
        </mj-text>
        <mj-text font-size="16px" color="#555">
            {{ _('We received a request to change password for user with email %(email)s. If it is not you please contact with service support') % {'email': email } }}
        </mj-text>
        <mj-text font-size="16px" color="#555">{{_("This is password change verification code")}}:</mj-text>
        <mj-text font-size="32px">{{ verification_code }}</mj-text>
        <mj-divider border-color="#555" border-width="2px" />
        <mj-text font-size="14px" color="#555">{{_("The password change verification code will expire in several minutes.")}}</mj-text>
        <mj-text font-size="14px" color="#555">{{_("If you didn't request a password change verification you can disregard this email.")}}</mj-text>
      </mj-column>
    </mj-section>
  </mj-body>
</mjml>
